<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EJS Setup</title>
    <link rel="stylesheet" href="/styles.css">
   
</head>
<body>
    <h1>Learning EJS with <%= name %></h1>
    <p><b>USER:</b> <%= userName %>, <b>AGE:</b><%= age%></p>
    <% if(age > 18){ %>
        <p>You are an adult</p>
    <% } else { %> 
        <p>You are a minor</p>
    <% }
    
    %>

    <h3>SKILLS</h3>
    <ul>
        <% skills.forEach(skill => { %>
            <li><%= skill %></li>
        <% });
        
        %>
    </ul>
</body>
</html>