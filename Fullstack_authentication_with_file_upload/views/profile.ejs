<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            height: 100vh;
            margin: 0;
            font-family: Arial, sans-serif;
        }
        h1 {
            text-align: center;
            margin-bottom: 20px;
        }
        a {
            margin-top: 20px;
            text-align: center;
            color: #007bff;
            text-decoration: none;
        }
        a:hover {
            text-decoration: underline;
        }
        img {
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <h1>Welcome, <%= user.username %>!</h1>
    <p>Email: <%= user.email %></p>

    <% if (user.imgUrl) { %>
        <img src="<%= user.imgUrl %>" alt="Profile Image" width="200">
    <% } else { %>
        <p>No profile picture uploaded.</p>
    <% } %>

    <a href="/">Logout</a>
    
</body>
</html>